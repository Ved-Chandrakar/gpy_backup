<!DOCTYPE html>
<html lang="hi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>पासवर्ड भूल गए - ग्रीन पालना योजना</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- Google Fonts for Hindi -->
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+Devanagari:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --primary-color: #28a745;
            --secondary-color: #20c997;
            --success-color: #198754;
            --danger-color: #dc3545;
            --warning-color: #ffc107;
            --info-color: #0dcaf0;
            --light-color: #f8f9fa;
            --dark-color: #212529;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Noto Sans Devanagari', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .forgot-container {
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            width: 100%;
            max-width: 500px;
            padding: 60px 50px;
            text-align: center;
            animation: fadeInUp 0.6s ease-out;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .forgot-header {
            margin-bottom: 40px;
        }

        .forgot-icon {
            font-size: 4rem;
            color: var(--primary-color);
            margin-bottom: 30px;
        }

        .forgot-title {
            font-size: 2.5rem;
            font-weight: 700;
            color: var(--dark-color);
            margin-bottom: 15px;
        }

        .forgot-subtitle {
            font-size: 1.1rem;
            color: #6c757d;
            margin-bottom: 30px;
            line-height: 1.6;
        }

        .form-floating {
            margin-bottom: 25px;
            position: relative;
        }

        .form-control {
            border: 2px solid #e9ecef;
            border-radius: 12px;
            padding: 25px 15px 10px 15px;
            font-size: 1rem;
            transition: all 0.3s ease;
            background: #f8f9fa;
            height: 60px;
            line-height: 1.5;
        }

        .form-control.has-icon {
            padding-left: 50px;
        }

        .form-control:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 0.2rem rgba(40, 167, 69, 0.25);
            background: white;
        }

        .form-control:focus + .form-label,
        .form-control:not(:placeholder-shown) + .form-label {
            transform: scale(0.85) translateY(-1.2rem) translateX(0.15rem);
            color: var(--primary-color);
        }

        .form-label {
            position: absolute;
            top: 50%;
            left: 15px;
            transform: translateY(-50%);
            font-weight: 600;
            color: #6c757d;
            font-size: 1rem;
            transition: all 0.3s ease;
            pointer-events: none;
            background: transparent;
            padding: 0 5px;
            z-index: 2;
        }

        .form-label.has-icon {
            left: 50px;
        }

        .btn-submit {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
            border: none;
            border-radius: 12px;
            padding: 15px 30px;
            font-size: 1.1rem;
            font-weight: 600;
            color: white;
            transition: all 0.3s ease;
            width: 100%;
            margin-bottom: 20px;
        }

        .btn-submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(40, 167, 69, 0.3);
        }

        .btn-back {
            background: transparent;
            border: 2px solid var(--primary-color);
            border-radius: 12px;
            padding: 12px 30px;
            font-size: 1rem;
            font-weight: 600;
            color: var(--primary-color);
            transition: all 0.3s ease;
            width: 100%;
            text-decoration: none;
            display: inline-block;
        }

        .btn-back:hover {
            background: var(--primary-color);
            color: white;
            transform: translateY(-2px);
        }

        .alert {
            border-radius: 12px;
            border: none;
            padding: 15px 20px;
            margin-bottom: 20px;
            font-weight: 500;
        }

        .alert-danger {
            background: linear-gradient(135deg, #f8d7da 0%, #f5c6cb 100%);
            color: #721c24;
        }

        .alert-success {
            background: linear-gradient(135deg, #d4edda 0%, #c3e6cb 100%);
            color: #155724;
        }

        .input-group {
            position: relative;
        }

        .input-icon {
            position: absolute;
            left: 18px;
            top: 50%;
            transform: translateY(-50%);
            color: #6c757d;
            z-index: 10;
            font-size: 1.1rem;
            transition: color 0.3s ease;
        }

        .form-control:focus ~ .input-icon {
            color: var(--primary-color);
        }

        .form-control.has-icon {
            padding-left: 50px;
        }

        .help-text {
            color: #6c757d;
            font-size: 0.9rem;
            margin-top: 20px;
            padding: 15px;
            background: #f8f9fa;
            border-radius: 8px;
            text-align: left;
        }

        .help-text i {
            color: var(--info-color);
            margin-right: 8px;
        }

        @media (max-width: 768px) {
            .forgot-container {
                padding: 40px 30px;
                margin: 20px;
            }

            .forgot-title {
                font-size: 2rem;
            }

            .forgot-icon {
                font-size: 3rem;
            }
        }
    </style>
</head>
<body>
    <div class="forgot-container">
        <div class="forgot-header">
            <div class="forgot-icon">
                <i class="fas fa-key"></i>
            </div>
            <h1 class="forgot-title">पासवर्ड भूल गए?</h1>
            <p class="forgot-subtitle">
                चिंता न करें! अपना मोबाइल नंबर दर्ज करें और हम आपको पासवर्ड रीसेट करने का लिंक भेजेंगे।
            </p>
        </div>

        <% if (flash && flash.error) { %>
            <div class="alert alert-danger d-flex align-items-center">
                <i class="fas fa-exclamation-circle me-2"></i>
                <%= flash.error %>
            </div>
        <% } %>

        <% if (flash && flash.success) { %>
            <div class="alert alert-success d-flex align-items-center">
                <i class="fas fa-check-circle me-2"></i>
                <%= flash.success %>
            </div>
        <% } %>

        <form id="forgotForm" action="/admin/forgot-password" method="POST">
            <div class="form-floating">
                <div class="input-group">
                    <i class="input-icon fas fa-mobile-alt"></i>
                    <input type="tel" class="form-control has-icon" id="mobile" name="mobile" 
                           placeholder=" " required maxlength="10" 
                           pattern="[0-9]{10}" title="कृपया 10 अंकों का वैध मोबाइल नंबर दर्ज करें">
                    <label for="mobile" class="form-label has-icon">मोबाइल नंबर</label>
                </div>
            </div>

            <button type="submit" class="btn btn-submit">
                <i class="fas fa-paper-plane me-2"></i>
                रीसेट लिंक भेजें
            </button>
        </form>

        <a href="/admin/login" class="btn-back">
            <i class="fas fa-arrow-left me-2"></i>
            लॉगिन पर वापस जाएं
        </a>

        <div class="help-text">
            <p><i class="fas fa-info-circle"></i> <strong>सहायता:</strong></p>
            <ul style="margin: 10px 0 0 20px;">
                <li>कृपया अपना पंजीकृत मोबाइल नंबर दर्ज करें</li>
                <li>आपको SMS के माध्यम से रीसेट लिंक मिलेगा</li>
                <li>यदि आपको SMS नहीं मिला तो कृपया स्पैम फ़ोल्डर चेक करें</li>
                <li>अधिक सहायता के लिए तकनीकी सहायता टीम से संपर्क करें</li>
            </ul>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        // Mobile number validation
        document.getElementById('mobile').addEventListener('input', function(e) {
            this.value = this.value.replace(/[^0-9]/g, '');
            if (this.value.length > 10) {
                this.value = this.value.slice(0, 10);
            }
            handleFloatingLabel(this);
        });

        // Handle floating labels
        function handleFloatingLabel(input) {
            const label = input.nextElementSibling;
            if (input.value.length > 0 || input === document.activeElement) {
                label.style.transform = 'scale(0.85) translateY(-1.2rem) translateX(0.15rem)';
                label.style.color = 'var(--primary-color)';
                if (input === document.activeElement) {
                    input.style.borderColor = 'var(--primary-color)';
                }
            } else {
                label.style.transform = 'translateY(-50%)';
                label.style.color = '#6c757d';
                input.style.borderColor = '#e9ecef';
            }
        }

        // Handle focus and blur events
        document.querySelectorAll('.form-control').forEach(input => {
            input.addEventListener('focus', function() {
                handleFloatingLabel(this);
            });
            
            input.addEventListener('blur', function() {
                handleFloatingLabel(this);
            });
        });

        // Form submission validation
        document.getElementById('forgotForm').addEventListener('submit', function(e) {
            const mobile = document.getElementById('mobile').value;

            if (!mobile || mobile.length !== 10) {
                e.preventDefault();
                alert('कृपया 10 अंकों का वैध मोबाइल नंबर दर्ज करें');
                return false;
            }
        });

        // Auto-focus on mobile field and initialize floating labels
        document.addEventListener('DOMContentLoaded', function() {
            document.getElementById('mobile').focus();
            
            // Initialize floating labels for any pre-filled inputs
            document.querySelectorAll('.form-control').forEach(input => {
                handleFloatingLabel(input);
            });
        });

        // Remove alerts after 5 seconds
        setTimeout(() => {
            const alerts = document.querySelectorAll('.alert');
            alerts.forEach(alert => {
                alert.style.transition = 'opacity 0.5s ease';
                alert.style.opacity = '0';
                setTimeout(() => {
                    alert.remove();
                }, 500);
            });
        }, 5000);
    </script>
</body>
</html>
